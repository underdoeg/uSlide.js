(function( $ ) {
  $.fn.uSlide = function(options) {
    
    //settings
    var settings = $.extend( {
        
    }, options);

    //execute plugin
    return this.each(function() {        
    });
  };
})( jQuery );